#________________________________________________________________________________________________________DST_PHYSICS__
#
PHYS_DST_STREAMING_EVENT_run2pp:

   params:
     name:       DST_STREAMING_EVENT_run2pp
     build:      new
     build_name: new
     dbtag:      2024p001
     logbase :   $(name)_$(build)_$(tag)-$INT(run,%08d)-$INT(seg,%04d)
     outbase :   $(name)_$(build)_$(tag)
     script  :   run_cosmics.sh
     payload :   ./slurp-examples/sPHENIX/cosmics/
     mem     :   4096MB
#    mem     :   16384MB
     neventsper: 10000
     comment:    "---"

   input:
      db: daqdb
      direct_path: /sphenix/lustre01/sphnxpro/{mode}/*/physics/
      query: |-
         select 
                'daqdb/filelist'                                                                                    as source      , 
                runnumber                                                                                                          , 
                0                                                                                                   as segment     , 
                string_agg( distinct split_part(filename,'/',-1), ' ' )                                             as files       ,   
                string_agg( distinct split_part(filename,'/',-1) || ':' || firstevent || ':' || lastevent, ' ' )    as fileranges  
         from  
                filelist
         where 
           ( 
             (filename  like '/bbox%/TPC%physics%.evt'   and lastevent>2 ) or
             (filename  like '/bbox%/TPOT%physics%.evt'  and lastevent>2 ) or
             (filename  like '/bbox%/physics_intt%.evt'  and lastevent>2 ) or
             (filename  like '/bbox%/GL1_physics%'       and lastevent>2 ) or
             (filename  like '/bbox%/physics_mvtx%.evt'  and lastevent>2 )
           )
                {run_condition}

         group by runnumber
         having
                every(transferred_to_sdcc)   and
                max(lastevent)>1000          and
                sum( case when filename like '/bbox%/GL1_physics%' then 1 else 0 end )>0 and
                (
                   sum( case when filename like '/bbox%/TPC%physics%' then 1 else 0 end )>0 or
                   sum( case when filename like '/bbox%/TPOT%physics%' then 1 else 0 end )>0 or
                   sum( case when filename like '/bbox%/physics%intt%' then 1 else 0 end )>0 or
                   sum( case when filename like '/bbox%/physics_mvtx%.evt' then 1 else 0 end )>0 
                )
         order by runnumber
                {limit_condition}
              ;

   # TODO:  Need to add error checking to make sure that outdir, logdir, etc... are quoted properly.  Else, this will cause problems with argument substitution
   filesystem:  
     outdir  : "/sphenix/lustre01/sphnxpro/physics/slurp/streaming/physics/run_$(rungroup)"
     logdir  : "file:///sphenix/data/data02/sphnxpro/streaminglogs/run_$(rungroup)"
     condor :        "/tmp/testlogs/run_$(rungroup)"

   #
   # Again I note the need to ensure that the arguments are properly specified given the
   # definition of the payload script.
   #
   job:
     executable            : "{payload}/run_cosmics.sh"
     arguments             : "$(nevents) {outbase} {logbase} $(run) $(seg) {outdir} $(build) $(tag) $(inputs) $(ranges) {neventsper} {logdir} {comment}"
     output_destination    : '{logdir}'
     transfer_input_files  : "{payload},cups.py,odbc.ini"
     output                : '{logbase}.condor.stdout'
     error                 : '{logbase}.condor.stderr'
     log                   : '{condor}/{logbase}.condor'
     accounting_group      : "group_sphenix.mdc2"
     accounting_group_user : "sphnxpro"
#    transfer_output_files : '{logbase}.out,{logbase}.err'
     transfer_output_files : ""
#    transfer_output_files : 'stdout.log,stderr.log'
     priority : '3800'



#_______________________________________________________________________________________________________DST_EVENT__
#
# kaedama.py --rule DST_EVENT --config examples/sPHENIX/DST_EVENT_aua23.yaml --runs ...
#
DST_TPCCALIB:

   # DST_EVENT works from a pre-built set of run lists.
   params:
     name:       DST_TPC_EVENT_run2pp
     build:      new
     build_name: new
     dbtag:      2024p001
     logbase :   $(name)_$(build)_$(tag)-$INT(run,%08d)-$INT(seg,%04d)
     outbase :   $(name)_$(build)_$(tag)
     script  :   run_cosmics.sh
     payload :   ./slurp-examples/sPHENIX/cosmics/
     mem     :   2048MB
     neventsper: 50000
#    mem     :   8192MB
     mem     :   16384MB

   #
   # input query:
   #
   # This builds a list of all runs known to the file catalog "datasets" table.
   # The query should return:
   # 1. The source of the information (formatted as database name/table name)
   # 2. The run number
   # 3. A sequence number (a placeholder fixed at zero for event builders)
   # 4. And a space-separated list of logical filenames 
   #
   # The {*_condition} parameters (run, seg, limit) are substituted by kaedama
   # based on (optional) command line options.
   #
   input:
      db: daqdb
      direct_path: /sphenix/lustre01/sphnxpro/{mode}/tpc/calib/
      query: |-
         select 
                'daqdb/filelist'                                                                                    as source      , 
                runnumber                                                                                                          , 
                0                                                                                                   as segment     , 
                string_agg( distinct split_part(filename,'/',-1), ' ' )                                             as files       ,   
                string_agg( distinct split_part(filename,'/',-1) || ':' || firstevent || ':' || lastevent, ' ' )    as fileranges  
         from  
                filelist
         where 
                filename     like '/bbox%/TPC%calib%.evt'
                {run_condition}
         group by runnumber
         having
                every(transferred_to_sdcc)                                            
         order by runnumber
                {limit_condition}
              ;              

   # TODO:  Need to add error checking to make sure that outdir, logdir, etc... are quoted properly.  Else, this will cause problems with argument substitution
   filesystem:  
     outdir : "/sphenix/lustre01/sphnxpro/physics/slurp/streaming/calib/run_$(rungroup)"
     logdir : "file:///sphenix/data/data02/sphnxpro/streaming/caliblogs/run_$(rungroup)"
     condor :        "/tmp/testlogs/run_$(rungroup)"

   #
   # Again I note the need to ensure that the arguments are properly specified given the
   # definition of the payload script.
   #
   job:
     executable            : "{payload}/run_cosmics.sh"
     arguments             : "$(nevents) {outbase} {logbase} $(run) $(seg) {outdir} $(build) $(tag) $(inputs) $(ranges) {neventsper} {logdir}"
     output_destination    : '{logdir}'
     transfer_input_files  : "{payload},cups.py,odbc.ini"
     output                : '{logbase}.condor.stdout'
     error                 : '{logbase}.condor.stderr'
     log                   : '{condor}/{logbase}.condor'
     accounting_group      : "group_sphenix.mdc2"
     accounting_group_user : "sphnxpro"
     transfer_output_files : ""
#    transfer_output_files : '{logbase}.out,{logbase}.err'
#    transfer_output_files : 'stdout.log,stderr.log'
     priority : '3800'





DST_INTT_EVENT:

   # At the moment, an extra _ in here will throw off cups b/c it parses out 
   # run, build, dsttype, etc... from the output filename... so DST_PHYSICS_run1aua
   # will need to wait...
   params:
     name:       DST_INTT_EVENT_physics
     build:      new
     build_name: new
     dbtag:      2024p001
     logbase :   $(name)_$(build)_$(tag)-$INT(run,%08d)-$INT(seg,%04d)
     outbase :   $(name)_$(build)_$(tag)
     script  :   run_cosmics.sh
     payload :   ./slurp-examples/sPHENIX/cosmics/
     mem     :   8192MB
     neventsper: 500000

   #
   # input query:
   #
   # This builds a list of all runs known to the file catalog "datasets" table.
   # The query should return:
   # 1. The source of the information (formatted as database name/table name)
   # 2. The run number
   # 3. A sequence number (a placeholder fixed at zero for event builders)
   # 4. And a space-separated list of logical filenames 
   #
   # The {*_condition} parameters (run, seg, limit) are substituted by kaedama
   # based on (optional) command line options.
   #
   # For now, require GL1 files...
   #             (filename  like '/bbox%/physics_mvtx%.evt'  and lastevent>2 )
   #               sum( case when filename like '/bbox%/physics%mvtx%' then 1 else 0 end )>0 
   #
   input:
      db: daqdb
      direct_path: /sphenix/lustre01/sphnxpro/{mode}/*/physics/
      query: |-
         select 
                'daqdb/filelist'                                                                                    as source      , 
                runnumber                                                                                                          , 
                0                                                                                                   as segment     , 
                string_agg( distinct split_part(filename,'/',-1), ' ' )                                             as files       ,   
                string_agg( distinct split_part(filename,'/',-1) || ':' || firstevent || ':' || lastevent, ' ' )    as fileranges  
         from  
                filelist
         where 
           ( 
             (filename  like '/bbox%/TPOT%physics%.evt'  and lastevent>2 ) or
             (filename  like '/bbox%/physics_intt%.evt'  and lastevent>2 ) or
             (filename  like '/bbox%/GL1_physics%'       and lastevent>2 ) or
             (filename  like '/bbox%/physics_mvtx%.evt'  and lastevent>2 )
           )
                {run_condition}
           and runnumber>=35590

         group by runnumber
         having
                every(transferred_to_sdcc)   and
                max(lastevent)>1000          and
                sum( case when filename like '/bbox%/GL1_physics%' then 1 else 0 end )>0 and
                (
                   sum( case when filename like '/bbox%/TPOT%physics%' then 1 else 0 end )>0 or
                   sum( case when filename like '/bbox%/physics%intt%' then 1 else 0 end )>0 or
                   sum( case when filename like '/bbox%/physics_mvtx%.evt' then 1 else 0 end )>0 
                )
         order by runnumber
                {limit_condition}
              ;              

   # TODO:  Need to add error checking to make sure that outdir, logdir, etc... are quoted properly.  Else, this will cause problems with argument substitution
   filesystem:  
     outdir : "/sphenix/lustre01/sphnxpro/physics/slurp/inttphysics/run_$(rungroup)"
     logdir : "file:///sphenix/data/data02/sphnxpro/testlogs/run_$(rungroup)"
     condor :        "/tmp/testlogs/run_$(rungroup)"

   #
   # Again I note the need to ensure that the arguments are properly specified given the
   # definition of the payload script.
   #
   job:
     executable            : "{payload}/run_cosmics.sh"
     arguments             : "$(nevents) {outbase} {logbase} $(run) $(seg) {outdir} $(build) $(tag) $(inputs) $(ranges) {neventsper} {logdir}"
     output_destination    : '{logdir}'
     transfer_input_files  : "{payload},cups.py,odbc.ini"
     output                : '{logbase}.condor.stdout'
     error                 : '{logbase}.condor.stderr'
     log                   : '{condor}/{logbase}.condor'
     accounting_group      : "group_sphenix.mdc2"
     accounting_group_user : "sphnxpro"
     transfer_output_files : ""
#    transfer_output_files : '{logbase}.out,{logbase}.err'
#    transfer_output_files : 'stdout.log,stderr.log'
     priority : '3800'




#______________________________________________________________________________________________DST_PHYSICS_TRIGGERED__
#
PHYS_DST_TRIGGERED_EVENT_run2pp:

   # At the moment, an extra _ in here will throw off cups b/c it parses out 
   # run, build, dsttype, etc... from the output filename... so DST_PHYSICS_run1aua
   # will need to wait...
   params:
     name:       DST_TRIGGERED_EVENT_run2pp
     build:      new
     build_name: new
     dbtag:      2024p001
     logbase :   $(name)_$(build)_$(tag)-$INT(run,%08d)-$INT(seg,%04d)
     outbase :   $(name)_$(build)_$(tag)
     script  :   run.sh
     payload :   ./slurp-examples/sPHENIX/triggers/
     mem     :   8192MB
     neventsper: 10000

   #
   # input query:
   #
   # This builds a list of all runs known to the file catalog "datasets" table.
   # The query should return:
   # 1. The source of the information (formatted as database name/table name)
   # 2. The run number
   # 3. A sequence number (a placeholder fixed at zero for event builders)
   # 4. And a space-separated list of logical filenames 
   #
   # The {*_condition} parameters (run, seg, limit) are substituted by kaedama
   # based on (optional) command line options.
   #
   # For now, require GL1 files...
   #
   input:
      db: daqdb
      direct_path: /sphenix/lustre01/sphnxpro/{mode}/*/physics/
      query: |-
         select 
                'daqdb/filelist'                                                                                    as source      , 
                runnumber                                                                                                          , 
                0                                                                                                   as segment     , 
                string_agg( distinct split_part(filename,'/',-1), ' ' )                                             as files       ,   
                string_agg( distinct split_part(filename,'/',-1) || ':' || firstevent || ':' || lastevent, ' ' )    as fileranges  
         from  
                filelist
         where 
           ( 
             (filename  like '/bbox%/%emcal%physics%'    and lastevent>2 ) or 
             (filename  like '/bbox%/%HCal%physics%'     and lastevent>2 ) or
             (filename  like '/bbox%/%LL1%physics%'      and lastevent>2 ) or
             (filename  like '/bbox%/GL1_physics%'       and lastevent>2 ) or
             (filename  like '/bbox%/%mbd%physics%'      and lastevent>2 ) or
             (filename  like '/bbox%/%ZDC%physics%'      and lastevent>2 )
           )
           {run_condition}

         group by runnumber

         having
                every(transferred_to_sdcc)   and
                max(lastevent)>1000          and
                sum( case when filename like '/bbox%/GL1_physics%' then 1 else 0 end )>0 and
                (
                   sum( case when filename like '/bbox%/%emcal%physics%'  then 1 else 0 end )>0 or
                   sum( case when filename like '/bbox%/%HCal%physics%'   then 1 else 0 end )>0 
                )

         order by runnumber
                {limit_condition}

              ;              

   # TODO:  Need to add error checking to make sure that outdir, logdir, etc... are quoted properly.  Else, this will cause problems with argument substitution
   filesystem:  
     outdir : "/sphenix/lustre01/sphnxpro/physics/slurp/calophysics/run_$(rungroup)"
     logdir : "file:///sphenix/data/data02/sphnxpro/calologs/run_$(rungroup)"
     histdir :       "/sphenix/data/data02/sphnxpro/calohist/run_$(rungroup)"
     condor :        "/tmp/calologs/run_$(rungroup)"

   #
   # Again I note the need to ensure that the arguments are properly specified given the
   # definition of the payload script.
   #
   job:
     executable            : "{payload}/run.sh"
     arguments             : "$(nevents) {outbase} {logbase} $(run) $(seg) {outdir} $(build) $(tag) $(inputs) $(ranges) {neventsper} {logdir} {histdir}"
     output_destination    : '{logdir}'
     transfer_input_files  : "{payload},cups.py,odbc.ini"
     output                : '{logbase}.condor.stdout'
     error                 : '{logbase}.condor.stderr'
     log                   : '{condor}/{logbase}.condor'
     accounting_group      : "group_sphenix.mdc2"
     accounting_group_user : "sphnxpro"
     priority : '3800'
     transfer_output_files : ""

#___________________________________________________________________________________________________DST_CALO_run2pp __     
# Downstream production of the 2024 calorimeter data
DST_CALO_Y2:
   params:
     name:       DST_CALO_run2pp
     build:      new
     build_name: new
     dbtag:      2024p001
     logbase :   $(name)_$(build)_$(tag)-$INT(run,%08d)-$INT(seg,%04d)
     outbase :   $(name)_$(build)_$(tag)
     script  :   runy2.sh
     payload :   ./slurp-examples/sPHENIX/CaloProduction/
     mem     :   4096MB
     neventsper: 50000
   input:
     db: filecatalog
     query: |-
         select 
                'filecatalog/datasets'                                                                                    as source      , 
                runnumber                                                                                                          , 
                segment                                                                                                            ,
                filename                                                                                            as files       ,
                filename || ':' || 0 || ':' || events                                                   as fileranges 
         from  
                datasets
         where 
                filename like 'DST_TRIGGERED_EVENT_run2pp%'
                {run_condition}
           
         {limit_condition}
         ;
   job:
     executable            : "{payload}/runy2.sh"
     arguments             : "$(nevents) {outbase} {logbase} $(run) $(seg) {outdir} $(build) $(tag) $(inputs) $(ranges) {neventsper} {logdir} {histdir}"
     output_destination    : '{logdir}'
     transfer_input_files  : "{payload},cups.py,odbc.ini"
     output                : '{logbase}.condor.stdout'
     error                 : '{logbase}.condor.stderr'
     log                   : '{condor}/{logbase}.condor'
     accounting_group      : "group_sphenix.mdc2"
     accounting_group_user : "sphnxpro"
     priority : '3800'
     transfer_output_files : ""


   filesystem:  
     outdir : "/sphenix/lustre01/sphnxpro/physics/slurp/caloy2test/run_$(rungroup)"
     logdir : "file:///sphenix/data/data02/sphnxpro/caloy2test/run_$(rungroup)"
     histdir :       "/sphenix/data/data02/sphnxpro/calohist/run_$(rungroup)"
     condor :        "/tmp/caloy2test/run_$(rungroup)"



DST_TRKR_HIT_SET_physics:
   # DST_EVENT works from a pre-built set of run lists.
   params:
     name:       DST_TRKR_HIT_run2pp
     build:      new
     build_name: new
     dbtag:      2024p001
     logbase :   $(name)_$(build)_$(tag)-$INT(run,%08d)-$INT(seg,%04d)
     outbase :   $(name)_$(build)_$(tag)
     script  :   run.sh
     payload :   ./slurp-examples/sPHENIX/TrackingProduction/
     mem     :   2048MB

   input:
      db: fc
      query: |-
         select 
                'filecatalog/datasets'   as source       ,
                runnumber                                ,
                segment                                  ,
                filename                    as files     ,
                filename || ':0:' || events as fileranges
         from  
                datasets
         where
                filename like 'DST_STREAMING_EVENT_run2pp_new_2024p001%'
                {run_condition}
         order by runnumber
                {limit_condition}
              ;              
   filesystem:  
     outdir : "/sphenix/lustre01/sphnxpro/physics/slurp/tracking/run_$(rungroup)"
     logdir : "file:///sphenix/data/data02/sphnxpro/trackinglogs/run_$(rungroup)"
     condor :        "/tmp/trkrogs/run_$(rungroup)"

   job:
     executable            : "{payload}/run.sh"
     arguments             : "$(nevents) {outbase} {logbase} $(run) $(seg) {outdir} $(build) $(tag) $(inputs) $(ranges) {logdir}"
     output_destination    : '{logdir}'
     transfer_input_files  : "{payload},cups.py,odbc.ini"
     output                : '{logbase}.condor.stdout'
     error                 : '{logbase}.condor.stderr'
     log                   : '{condor}/{logbase}.condor'
     accounting_group      : "group_sphenix.mdc2"
     accounting_group_user : "sphnxpro"
     transfer_output_files : ""
     priority : '3800'




#_________________________________________________________________________________________________________________________________________
DST_TRKR_CLUSTER_SET_run2pp:
   # DST_EVENT works from a pre-built set of run lists.
   params:
     name:       DST_TRKR_CLUSTER_run2pp
     build:      new
     build_name: new
     dbtag:      2024p001
     logbase :   $(name)_$(build)_$(tag)-$INT(run,%08d)-$INT(seg,%04d)
     outbase :   $(name)_$(build)_$(tag)
     script  :   run_job0.sh
     payload :   ./slurp-examples/sPHENIX/TrackingProduction/
     mem     :   2048MB
     nevents :  0

   input:
      db: fc
      query: |-
         select 
                'filecatalog/datasets'   as source       ,
                runnumber                                ,
                segment                                  ,
                filename                    as files     ,
                filename || ':0:' || events as fileranges
         from  
                datasets
         where
                filename like 'DST_TRKR_HIT_run2pp_new_2024p001%'
                {run_condition}
         order by runnumber
                {limit_condition}
              ;              
   filesystem:  
     outdir : "/sphenix/lustre01/sphnxpro/physics/slurp/tracking/run_$(rungroup)"
     logdir : "file:///sphenix/data/data02/sphnxpro/trackinglogs/run_$(rungroup)"
     histdir :       "/sphenix/data/data02/sphnxpro/clusterhist/run_$(rungroup)"
     condor :        "/tmp/trkrlogs/run_$(rungroup)"

   job:
     executable            : "{payload}/run_job0.sh"
     arguments             : "{nevents} {outbase} {logbase} $(run) $(seg) {outdir} $(build) $(tag) $(inputs) $(ranges) {logdir} {histdir}"
     output_destination    : '{logdir}'
     transfer_input_files  : "{payload},cups.py,odbc.ini"
     output                : '{logbase}.condor.stdout'
     error                 : '{logbase}.condor.stderr'
     log                   : '{condor}/{logbase}.condor'
     accounting_group      : "group_sphenix.mdc2"
     accounting_group_user : "sphnxpro"
     transfer_output_files : ""
     priority : '3800'



#_________________________________________________________________________________________________________________________________________
DST_TRKR_SEED_SET_run2pp:
   # DST_EVENT works from a pre-built set of run lists.
   params:
     name:       DST_TRKR_SEED_run2pp
     build:      new
     build_name: new
     dbtag:      2024p001
     logbase :   $(name)_$(build)_$(tag)-$INT(run,%08d)-$INT(seg,%04d)
     outbase :   $(name)_$(build)_$(tag)
     script  :   run_jobA.sh
     payload :   ./slurp-examples/sPHENIX/TrackingProduction/
     mem     :   2048MB
     nevents :  0

   input:
      db: fc
      query: |-
         select 
                'filecatalog/datasets'   as source       ,
                runnumber                                ,
                segment                                  ,
                filename                    as files     ,
                filename || ':0:' || events as fileranges
         from  
                datasets
         where
                filename like 'DST_TRKR_CLUSTER_run2pp_new_2024p001%'
                {run_condition}
         order by runnumber
                {limit_condition}
              ;              
   filesystem:  
     outdir : "/sphenix/lustre01/sphnxpro/physics/slurp/tracking/run_$(rungroup)"
     logdir : "file:///sphenix/data/data02/sphnxpro/trackinglogs/run_$(rungroup)"
     condor :        "/tmp/trkrlogs/run_$(rungroup)"

   job:
     executable            : "{payload}/run_jobA.sh"
     arguments             : "{nevents} {outbase} {logbase} $(run) $(seg) {outdir} $(build) $(tag) $(inputs) $(ranges) {logdir}"
     output_destination    : '{logdir}'
     transfer_input_files  : "{payload},cups.py,odbc.ini"
     output                : '{logbase}.condor.stdout'
     error                 : '{logbase}.condor.stderr'
     log                   : '{condor}/{logbase}.condor'
     accounting_group      : "group_sphenix.mdc2"
     accounting_group_user : "sphnxpro"
     transfer_output_files : ""
     priority : '3800'





DST_TRKR_TRACKS_SET_run2pp:
   # DST_EVENT works from a pre-built set of run lists.
   params:
     name:       DST_TRKR_TRACKS_run2pp
     build:      new
     build_name: new
     dbtag:      2024p001
     logbase :   $(name)_$(build)_$(tag)-$INT(run,%08d)-$INT(seg,%04d)
     outbase :   $(name)_$(build)_$(tag)
     script  :   run_jobC.sh
     payload :   ./slurp-examples/sPHENIX/TrackingProduction/
     mem     :   2048MB
     nevents :   0

   input:
      db: fc
      query: |-
         select
               'filecatalog/datasets'   as source       ,
               runnumber                                ,
               segment                                  ,
               string_agg( distinct split_part(filename,'/',-1), ' ' )                                             as files       ,   
               string_agg( distinct split_part(filename,'/',-1) || ':' || 0 || ':' || -1, ' ' )    as fileranges  
         from
               datasets
         where
           ( 
              filename like 'DST_TRKR_SEED_run2pp_new_2024p001%' or
              filename like 'DST_TRKR_CLUSTER_run2pp_new_2024p001%' 
           )
           {run_condition}

         group by runnumber,segment 

         having (

            sum( case when filename like 'DST_TRKR_SEED_run2pp_new_2024p001%' then 1 else 0 end )>0    and
            sum( case when filename like 'DST_TRKR_CLUSTER_run2pp_new_2024p001%' then 1 else 0 end )>0 

         )

         order by runnumber
 
         {limit_condition}
         ;
              ;              
   filesystem:  
     outdir : "/sphenix/lustre01/sphnxpro/physics/slurp/tracking/run_$(rungroup)"
     logdir : "file:///sphenix/data/data02/sphnxpro/trackinglogs//run_$(rungroup)"
     condor :        "/tmp/trkrlogs/run_$(rungroup)"

   job:
     executable            : "{payload}/run_jobC.sh"
     arguments             : "{nevents} {outbase} {logbase} $(run) $(seg) {outdir} $(build) $(tag) $(inputs) $(ranges) {logdir}"
     output_destination    : '{logdir}'
     transfer_input_files  : "{payload},cups.py,odbc.ini"
     output                : '{logbase}.condor.stdout'
     error                 : '{logbase}.condor.stderr'
     log                   : '{condor}/{logbase}.condor'
     accounting_group      : "group_sphenix.mdc2"
     accounting_group_user : "sphnxpro"
     transfer_output_files : ""
     priority : '3800'
